<p id="notice"><%= notice %></p>

<h1>Comentarios</h1>

<table>
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @interactions.each do |interaction| %>
      <tr>
        <td><%= link_to 'Mostrar', interaction %></td>
        <td><%= link_to 'Editar', edit_interaction_path(interaction) %></td>
        <td><%= link_to 'Eliminar', interaction, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Regresar a Documento', index_document_path %>
